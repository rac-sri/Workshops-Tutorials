---
- name: usage of verbosity of debug module
  hosts: localhost
  tasks:
    - name: verbosity as default
      debug:
        msg: "this is the default msg"
    - name: verbosity is 2
      debug:
        msg: "verbosity is 2"
        verbosity: 2
